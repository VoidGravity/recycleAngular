import { CollectionRequest } from './collection-request.model';

describe('CollectionRequest', () => {
  it('should create an instance', () => {
    expect(new CollectionRequest()).toBeTruthy();
  });
});
